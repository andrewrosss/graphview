[package]
name = "graphview"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.6", features = ["tracing"] }
handlebars = "4"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
